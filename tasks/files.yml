---
- name: Copying 'has_option' file for XOrg (fixes ssh-agent startup during X session)
  ansible.builtin.copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: root
    group: root
    mode: '0644'
  loop: "{{ files }}"
